# 旧稼働コンテナへ寄せるための、親リポジトリ管理の追加 Compose 設定です。
# データベース名・ユーザー・パスワードは configure-default-envs.sh 側で旧値を入れます。
services:
  wordpress:
    image: wordpress
    environment:
      WORDPRESS_DB_HOST: mysql:3306

  db:
    networks:
      default:
        aliases:
          - mysql
